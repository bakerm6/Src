class MyGameViewportClient extends GameViewportClient;

// Don't draw loading hints or "Precaching..."
function DrawTransition(Canvas Canvas)
{
    /*Canvas.Font = class'Engine'.Static.GetMediumFont(); 
    Canvas.SetPos(600,600);
    Canvas.SetDrawColor(0,255,0,255);
    Canvas.DrawText("Your heart rate is an indication of your health. Run away when you start to hear it!"); //Prompt is a string variable defined in our new actor's class.
*/}

defaultproperties
{
}